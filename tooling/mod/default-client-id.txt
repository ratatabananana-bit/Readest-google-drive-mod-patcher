# Bundled default Google OAuth client id (iOS type — Bundle ID only, NO secret,
# NO SHA-1). Committed on purpose: a client id is NOT a secret, and `drive.file`
# is a non-sensitive scope, so this ships a zero-setup build that signs every user
# into THEIR OWN Google Drive. A builder can override it in the patcher UI (writes
# tooling/mod/credentials.env, gitignored) to use their own client instead.
433627266031-77n0mfpr6e88vv80luhiafp9h07kh8lb.apps.googleusercontent.com
