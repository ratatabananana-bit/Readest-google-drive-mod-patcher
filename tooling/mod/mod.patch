diff --git a/apps/readest-app/public/locales/en/translation.json b/apps/readest-app/public/locales/en/translation.json
index ff49efd5..f8c38b28 100644
--- a/apps/readest-app/public/locales/en/translation.json
+++ b/apps/readest-app/public/locales/en/translation.json
@@ -70,11 +70,13 @@
   "Imported {{count}} annotations_other": "Imported {{count}} annotations",
   "Recently read": "Recently read",
   "Show recently read": "Show recently read",
+  "Hidden until you open it again": "Hidden until you open it again",
   "Your books will appear here": "Your books will appear here",
   "{{count}} results_one": "{{count}} result",
   "{{count}} results_other": "{{count}} results",
   "{{count}} uploads failed: insufficient storage quota_one": "{{count}} upload failed: insufficient storage quota",
   "{{count}} uploads failed: insufficient storage quota_other": "{{count}} uploads failed: insufficient storage quota",
   "Library sync via {{count}} providers_one": "Library sync via {{count}} provider",
-  "Library sync via {{count}} providers_other": "Library sync via {{count}} providers"
+  "Library sync via {{count}} providers_other": "Library sync via {{count}} providers",
+  "Connected to {{provider}}": "Connected to {{provider}}"
 }
diff --git a/apps/readest-app/src-tauri/tauri.conf.json b/apps/readest-app/src-tauri/tauri.conf.json
index 05a9be21..3e2672c0 100644
--- a/apps/readest-app/src-tauri/tauri.conf.json
+++ b/apps/readest-app/src-tauri/tauri.conf.json
@@ -1,8 +1,8 @@
 {
   "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
-  "productName": "Readest",
+  "productName": "Readest GMod",
   "mainBinaryName": "readest",
-  "identifier": "com.bilingify.readest",
+  "identifier": "com.readestgmod.app",
   "version": "../package.json",
   "build": {
     "frontendDist": "../out",
@@ -13,7 +13,10 @@
   "app": {
     "windows": [],
     "security": {
-      "capabilities": ["default", "desktop-capability"],
+      "capabilities": [
+        "default",
+        "desktop-capability"
+      ],
       "csp": {
         "default-src": "'self' 'unsafe-inline' blob: data: customprotocol: asset: http://asset.localhost http://rangefile.localhost ipc: http://ipc.localhost",
         "connect-src": "'self' blob: data: asset: http://asset.localhost http://rangefile.localhost ipc: http://ipc.localhost http://*:* https://*:* https://*.sentry.io https://*.posthog.com https://*.deepl.com https://*.wikipedia.org https://*.wiktionary.org https://*.supabase.co https://*.readest.com wss://speech.platform.bing.com https://*.cloudflarestorage.com https://translate.googleapis.com https://translate.toil.cc https://*.microsofttranslator.com https://edge.microsoft.com https://*.googleusercontent.com https://graph.microsoft.com https://login.microsoftonline.com",
@@ -79,49 +82,63 @@
     "fileAssociations": [
       {
         "name": "epub",
-        "ext": ["epub"],
+        "ext": [
+          "epub"
+        ],
         "description": "EPUB file",
         "mimeType": "application/epub+zip",
         "role": "Viewer"
       },
       {
         "name": "mobi",
-        "ext": ["mobi"],
+        "ext": [
+          "mobi"
+        ],
         "description": "MOBI file",
         "mimeType": "application/x-mobipocket-ebook",
         "role": "Viewer"
       },
       {
         "name": "azw",
-        "ext": ["azw"],
+        "ext": [
+          "azw"
+        ],
         "description": "AZW file",
         "mimeType": "application/vnd.amazon.ebook",
         "role": "Viewer"
       },
       {
         "name": "azw3",
-        "ext": ["azw3"],
+        "ext": [
+          "azw3"
+        ],
         "description": "AZW3 file",
         "mimeType": "application/vnd.amazon.mobi8-ebook",
         "role": "Viewer"
       },
       {
         "name": "fb2",
-        "ext": ["fb2"],
+        "ext": [
+          "fb2"
+        ],
         "description": "FB2 file",
         "mimeType": "application/x-fictionbook+xml",
         "role": "Viewer"
       },
       {
         "name": "cbz",
-        "ext": ["cbz"],
+        "ext": [
+          "cbz"
+        ],
         "description": "CBZ file",
         "mimeType": "application/vnd.comicbook+zip",
         "role": "Viewer"
       },
       {
         "name": "pdf",
-        "ext": ["pdf"],
+        "ext": [
+          "pdf"
+        ],
         "description": "PDF file",
         "mimeType": "application/pdf",
         "role": "Viewer"
@@ -161,28 +178,35 @@
     "deep-link": {
       "mobile": [
         {
-          "scheme": ["https"],
+          "scheme": [
+            "https"
+          ],
           "host": "web.readest.com",
           "appLink": true
         },
         {
-          "scheme": ["readest"],
+          "scheme": [
+            "readest"
+          ],
           "appLink": false
         },
         {
-          "scheme": ["readest-onedrive"],
+          "scheme": [
+            "readest-onedrive"
+          ],
           "appLink": false
         },
         {
-          "scheme": ["com.googleusercontent.apps.209390247301-ctpmep68ppfa56r1b8tr35e4qi4p60kq"],
+          "scheme": [
+            "com.googleusercontent.apps.433627266031-77n0mfpr6e88vv80luhiafp9h07kh8lb"
+          ],
           "appLink": false
         }
       ],
       "desktop": {
         "schemes": [
           "readest",
-          "readest-onedrive",
-          "com.googleusercontent.apps.209390247301-ctpmep68ppfa56r1b8tr35e4qi4p60kq"
+          "com.googleusercontent.apps.433627266031-77n0mfpr6e88vv80luhiafp9h07kh8lb"
         ]
       }
     },
diff --git a/apps/readest-app/src/app/library/components/Bookshelf.tsx b/apps/readest-app/src/app/library/components/Bookshelf.tsx
index 495ad0ba..6481dd1a 100644
--- a/apps/readest-app/src/app/library/components/Bookshelf.tsx
+++ b/apps/readest-app/src/app/library/components/Bookshelf.tsx
@@ -712,8 +712,13 @@ const Bookshelf: React.FC<BookshelfProps> = ({
   // Flat recency slice of the whole library, independent of the main shelf's
   // sort/grouping. Built from `libraryBooks` (not the sorted/filtered items).
   const recentBooks = useMemo(
-    () => selectRecentShelfBooks(libraryBooks, RECENT_SHELF_BOOK_COUNT),
-    [libraryBooks],
+    () =>
+      selectRecentShelfBooks(
+        libraryBooks,
+        RECENT_SHELF_BOOK_COUNT,
+        settings.libraryRecentShelfHiddenAt,
+      ),
+    [libraryBooks, settings.libraryRecentShelfHiddenAt],
   );
 
   // A top-level quick-resume strip: hidden while searching, inside a group,
diff --git a/apps/readest-app/src/app/library/components/RecentShelf.tsx b/apps/readest-app/src/app/library/components/RecentShelf.tsx
index fcedf01f..8a975149 100644
--- a/apps/readest-app/src/app/library/components/RecentShelf.tsx
+++ b/apps/readest-app/src/app/library/components/RecentShelf.tsx
@@ -3,8 +3,12 @@ import { useCallback, useEffect, useRef, useState } from 'react';
 import { MdChevronLeft, MdChevronRight } from 'react-icons/md';
 import { Book } from '@/types/book';
 import { LibraryCoverFitType } from '@/types/settings';
+import { useEnv } from '@/context/EnvContext';
+import { useSettingsStore } from '@/store/settingsStore';
 import { useTranslation } from '@/hooks/useTranslation';
 import { useLongPress } from '@/hooks/useLongPress';
+import { saveSysSettings } from '@/helpers/settings';
+import { eventDispatcher } from '@/utils/event';
 import BookItem from './BookItem';
 
 /**
@@ -13,6 +17,9 @@ import BookItem from './BookItem';
  */
 export const RECENT_SHELF_BOOK_COUNT = 12;
 
+/** Confirmation toast lifetime after a book is hidden (ms). */
+const HIDE_TOAST_TIMEOUT_MS = 2000;
+
 interface RecentShelfProps {
   books: Book[];
   coverFit: LibraryCoverFitType;
@@ -44,12 +51,17 @@ type RecentSlideProps = Pick<
   | 'handleBookDownload'
   | 'showBookDetailsModal'
   | 'showTimeRemaining'
-> & { book: Book };
+> & {
+  book: Book;
+  /** Hide the book from the shelf until it is read again. */
+  onRequestHide: (book: Book) => void;
+};
 
 const RecentSlide: React.FC<RecentSlideProps> = ({
   book,
   coverFit,
   onOpenBook,
+  onRequestHide,
   handleBookUpload,
   handleBookDownload,
   showBookDetailsModal,
@@ -57,17 +69,26 @@ const RecentSlide: React.FC<RecentSlideProps> = ({
 }) => {
   // Pointer-based tap, exactly like the grid (`BookItem` stops click
   // propagation). A swipe-to-scroll moves past useLongPress's moveThreshold and
-  // cancels the tap, so horizontal scrolling never opens a book.
-  const { pressing, handlers } = useLongPress({ onTap: () => onOpenBook(book) }, [
-    book,
-    onOpenBook,
-  ]);
+  // cancels the tap, so horizontal scrolling never opens a book. Press-and-hold
+  // (or right-click) hides the book from the shelf until it is read again.
+  const { pressing, handlers } = useLongPress(
+    {
+      onTap: () => onOpenBook(book),
+      onLongPress: () => onRequestHide(book),
+      onContextMenu: () => onRequestHide(book),
+    },
+    [book, onOpenBook, onRequestHide],
+  );
 
   const handleKeyDown = (e: React.KeyboardEvent) => {
     if (e.key === 'Enter' || e.key === ' ') {
       e.preventDefault();
       onOpenBook(book);
     }
+    if (e.key === 'ContextMenu' || (e.shiftKey && e.key === 'F10')) {
+      e.preventDefault();
+      onRequestHide(book);
+    }
   };
 
   return (
@@ -125,6 +146,42 @@ const RecentShelf: React.FC<RecentShelfProps> = ({
   showTimeRemaining,
 }) => {
   const _ = useTranslation();
+  const { envConfig } = useEnv();
+  const { settings } = useSettingsStore();
+
+  // One hide per press: a hide disarms further hides until the pointer lifts, so
+  // when the row reflows and the next cover slides under a still-held finger it
+  // is not hidden in turn. Re-armed on pointer up/cancel (see the scroller).
+  const canHideRef = useRef(true);
+  const rearmHide = useCallback(() => {
+    canHideRef.current = true;
+  }, []);
+
+  const hideBook = useCallback(
+    async (book: Book) => {
+      const hiddenAt = {
+        ...settings.libraryRecentShelfHiddenAt,
+        [book.hash]: Date.now(),
+      };
+      await saveSysSettings(envConfig, 'libraryRecentShelfHiddenAt', hiddenAt);
+    },
+    [envConfig, settings.libraryRecentShelfHiddenAt],
+  );
+
+  const requestHide = useCallback(
+    async (book: Book) => {
+      if (!canHideRef.current) return; // already hid one this press — wait for a lift
+      canHideRef.current = false;
+      await hideBook(book);
+      eventDispatcher.dispatch('toast', {
+        type: 'info',
+        message: _('Hidden until you open it again'),
+        timeout: HIDE_TOAST_TIMEOUT_MS,
+      });
+    },
+    [_, hideBook],
+  );
+
   // `--rs-cols` mirrors the grid's column count: the responsive ladder
   // (BOOKSHELF_GRID_CLASSES) when auto, or the fixed setting otherwise.
   // `--rs-gap` mirrors the grid's `gap-x-4 sm:gap-x-0` so the width formula
@@ -189,6 +246,8 @@ const RecentShelf: React.FC<RecentShelfProps> = ({
         <div
           ref={scrollerRef}
           onScroll={updateArrows}
+          onPointerUp={rearmHide}
+          onPointerCancel={rearmHide}
           className='no-scrollbar overflow-x-auto overflow-y-hidden overscroll-x-contain px-4 sm:px-2'
         >
           <div
@@ -201,6 +260,7 @@ const RecentShelf: React.FC<RecentShelfProps> = ({
                 book={book}
                 coverFit={coverFit}
                 onOpenBook={onOpenBook}
+                onRequestHide={requestHide}
                 handleBookUpload={handleBookUpload}
                 handleBookDownload={handleBookDownload}
                 showBookDetailsModal={showBookDetailsModal}
diff --git a/apps/readest-app/src/app/library/components/SettingsMenu.tsx b/apps/readest-app/src/app/library/components/SettingsMenu.tsx
index b5f452c5..d63f0357 100644
--- a/apps/readest-app/src/app/library/components/SettingsMenu.tsx
+++ b/apps/readest-app/src/app/library/components/SettingsMenu.tsx
@@ -4,7 +4,7 @@ import { useRouter } from 'next/navigation';
 import { PiUserCircle, PiUserCircleCheck, PiGear } from 'react-icons/pi';
 import { PiSun, PiMoon } from 'react-icons/pi';
 import { TbSunMoon } from 'react-icons/tb';
-import { MdCloudSync, MdSync, MdSyncProblem } from 'react-icons/md';
+import { MdCloudDone, MdCloudSync, MdSync, MdSyncProblem } from 'react-icons/md';
 
 import { isTauriAppPlatform, isWebAppPlatform } from '@/services/environment';
 import { DOWNLOAD_READEST_URL } from '@/services/constants';
@@ -316,6 +316,12 @@ const SettingsMenu: React.FC<SettingsMenuProps> = ({ onPullLibrary, setIsDropdow
       ? _('Synced {{time}}', { time: dayjs(lastSyncTime).fromNow() })
       : _('Never synced');
 
+  // A third-party cloud is connected exactly when a file-sync backend is ready
+  // to sync; this un-gates the "signed-in"-style account block (transfers + sync
+  // row) for a cloud-only user who has no Readest account.
+  const isCloudConnected = backends.length > 0;
+  const connectedCloudName = cloudProvidersDisplayName(backends);
+
   return (
     <Menu
       className={clsx(
@@ -324,20 +330,26 @@ const SettingsMenu: React.FC<SettingsMenuProps> = ({ onPullLibrary, setIsDropdow
       )}
       onCancel={() => setIsDropdownOpen?.(false)}
     >
-      {user ? (
+      {user || isCloudConnected ? (
         <MenuItem
           label={
-            userDisplayName
-              ? _('Logged in as {{userDisplayName}}', { userDisplayName })
-              : _('Logged in')
+            user
+              ? userDisplayName
+                ? _('Logged in as {{userDisplayName}}', { userDisplayName })
+                : _('Logged in')
+              : _('Connected to {{provider}}', { provider: connectedCloudName })
           }
           labelClass='!max-w-40'
-          aria-label={_('View account details and quota')}
+          aria-label={user ? _('View account details and quota') : undefined}
           Icon={
-            avatarUrl ? (
-              <UserAvatar url={avatarUrl} size={iconSize} DefaultIcon={PiUserCircleCheck} />
+            user ? (
+              avatarUrl ? (
+                <UserAvatar url={avatarUrl} size={iconSize} DefaultIcon={PiUserCircleCheck} />
+              ) : (
+                PiUserCircleCheck
+              )
             ) : (
-              PiUserCircleCheck
+              MdCloudDone
             )
           }
         >
@@ -359,7 +371,7 @@ const SettingsMenu: React.FC<SettingsMenuProps> = ({ onPullLibrary, setIsDropdow
             />
             <MenuItem
               label={syncRowLabel}
-              Icon={user ? MdSync : MdSyncProblem}
+              Icon={user || isCloudConnected ? MdSync : MdSyncProblem}
               labelClass='ps-2 pe-1 !mx-0'
               iconClassName={(user && isSyncing) || providerSyncing ? 'animate-reverse-spin' : ''}
               onClick={handleSyncLibrary}
@@ -386,7 +398,7 @@ const SettingsMenu: React.FC<SettingsMenuProps> = ({ onPullLibrary, setIsDropdow
                 <Quota quotas={quotas} labelClassName='h-10 pl-3 pr-2' />
               </button>
             ) : null}
-            <MenuItem label={_('Account')} onClick={handleUserProfile} />
+            {user && <MenuItem label={_('Account')} onClick={handleUserProfile} />}
           </ul>
         </MenuItem>
       ) : (
diff --git a/apps/readest-app/src/app/library/page.tsx b/apps/readest-app/src/app/library/page.tsx
index e74e07b0..136611e0 100644
--- a/apps/readest-app/src/app/library/page.tsx
+++ b/apps/readest-app/src/app/library/page.tsx
@@ -93,6 +93,7 @@ import {
   ensureLibraryGroupByType,
   findGroupById,
   getBreadcrumbs,
+  requiresSignInToSyncLibrary,
 } from './utils/libraryUtils';
 import Spinner from '@/components/Spinner';
 import LibraryHeader from './components/LibraryHeader';
@@ -309,10 +310,14 @@ const LibraryPageContent = ({ searchParams }: { searchParams: ReadonlyURLSearchP
   useInboxDrainer();
   const { isDragging } = useDragDropImport();
 
+  // Pulling down means "sync now". It only diverts to the login screen when
+  // signing in is the ONLY way this refresh could sync anything — `pullLibrary`
+  // runs its file pass without a Readest account, so a user synced to their own
+  // cloud must get that sync rather than a sign-in prompt.
   usePullToRefresh(
     scrollRef,
     async () => {
-      if (!user) {
+      if (requiresSignInToSyncLibrary(settings, !!user)) {
         navigateToLogin(router);
         return;
       }
@@ -320,7 +325,7 @@ const LibraryPageContent = ({ searchParams }: { searchParams: ReadonlyURLSearchP
       checkOPDSSubscriptions(true);
     },
     async () => {
-      if (!user) {
+      if (requiresSignInToSyncLibrary(settings, !!user)) {
         navigateToLogin(router);
         return;
       }
diff --git a/apps/readest-app/src/app/library/utils/libraryUtils.ts b/apps/readest-app/src/app/library/utils/libraryUtils.ts
index e36552aa..f1bc26ae 100644
--- a/apps/readest-app/src/app/library/utils/libraryUtils.ts
+++ b/apps/readest-app/src/app/library/utils/libraryUtils.ts
@@ -3,7 +3,9 @@ import {
   LibraryGroupByType,
   LibrarySecondarySortByType,
   LibrarySortByType,
+  type SystemSettings,
 } from '@/types/settings';
+import { getActiveFileSyncBackends } from '@/services/sync/cloudSyncProvider';
 import { formatAuthors, formatTitle, isCurrentlyReadingBook } from '@/utils/book';
 import { md5Fingerprint } from '@/utils/md5';
 import { SIZE_PER_LOC, SIZE_PER_TIME_UNIT } from '@/services/constants';
@@ -341,6 +343,15 @@ export const createBookSorter =
     return compareBookByKey(a, b, secondarySortBy, uiLanguage);
   };
 
+/**
+ * Whether a book should appear in the recently-read shelf: it must be currently
+ * being read (see `isCurrentlyReadingBook`) and not currently hidden — a book is
+ * hidden while its `updatedAt` hasn't moved past the moment the user hid it;
+ * reading it again bumps `updatedAt` so it returns to the shelf.
+ */
+const isRecentShelfBookVisible = (book: Book, hiddenAt: Record<string, number>): boolean =>
+  isCurrentlyReadingBook(book) && book.updatedAt > (hiddenAt[book.hash] ?? 0);
+
 /**
  * Pick the books for the recently-read shelf: most-recently-read first, capped
  * at `count`. Only currently-reading books qualify (see `isCurrentlyReadingBook`):
@@ -349,15 +360,36 @@ export const createBookSorter =
  * existing sort convention. NB: `updatedAt` is last-modified (also bumped by
  * status/metadata edits and sync), not strictly last-read. Independent of the
  * main shelf's sort/grouping — always a flat, recency slice.
+ *
+ * @param hiddenAt - Per-book hide timestamps (`libraryRecentShelfHiddenAt`);
+ *   a book is excluded until its `updatedAt` moves past its entry here.
  */
-export const selectRecentShelfBooks = (books: Book[], count: number): Book[] => {
+export const selectRecentShelfBooks = (
+  books: Book[],
+  count: number,
+  hiddenAt: Record<string, number> = {},
+): Book[] => {
   const byRecency = createBookSorter(LibrarySortByType.Updated, '');
   return books
-    .filter(isCurrentlyReadingBook)
+    .filter((book) => isRecentShelfBookVisible(book, hiddenAt))
     .sort((a, b) => -byRecency(a, b))
     .slice(0, count);
 };
 
+/**
+ * Whether a library refresh can only sync by signing into a Readest account —
+ * i.e. whether a refresh surface should show the login screen instead of
+ * syncing. True only when there is no account AND no third-party file backend:
+ * `pullLibrary`'s file pass has no Readest-account dependency, so a user who
+ * connected their own cloud (Google Drive / WebDAV / S3 / OneDrive) and has no
+ * Readest account must get their sync, not a login prompt. Gating on `user`
+ * alone would replace the sync they asked for with a sign-in they don't need.
+ */
+export const requiresSignInToSyncLibrary = (
+  settings: SystemSettings | null | undefined,
+  isSignedIn: boolean,
+): boolean => !isSignedIn && getActiveFileSyncBackends(settings).length === 0;
+
 /**
  * Build a `groupName -> max(book.updatedAt)` map for all groups touched by
  * the given books. Each book bumps both its direct group and every ancestor
diff --git a/apps/readest-app/src/services/constants.ts b/apps/readest-app/src/services/constants.ts
index 1d5bade4..da4526d8 100644
--- a/apps/readest-app/src/services/constants.ts
+++ b/apps/readest-app/src/services/constants.ts
@@ -179,6 +179,7 @@ export const DEFAULT_SYSTEM_SETTINGS: Partial<SystemSettings> = {
   libraryAutoColumns: true,
   libraryColumns: 6,
   libraryRecentShelfEnabled: false,
+  libraryRecentShelfHiddenAt: {},
 
   metadataSeriesCollapsed: false,
   metadataOthersCollapsed: false,
diff --git a/apps/readest-app/src/types/settings.ts b/apps/readest-app/src/types/settings.ts
index 89394163..432f65a0 100644
--- a/apps/readest-app/src/types/settings.ts
+++ b/apps/readest-app/src/types/settings.ts
@@ -369,6 +369,12 @@ export interface SystemSettings {
   libraryColumns: number;
   /** Show the recently-read carousel at the top of the library (issue #3797). */
   libraryRecentShelfEnabled: boolean;
+  /**
+   * Per-book timestamps (keyed by book hash) at which the user hid a book from
+   * the recently-read shelf. A book stays hidden until it is read again and its
+   * `updatedAt` moves past this moment (see `isRecentShelfBookVisible`).
+   */
+  libraryRecentShelfHiddenAt: Record<string, number>;
   /**
    * Library page background texture, configured independently from the reader
    * background (issue #4743). When any of these is undefined the library
diff --git a/apps/readest-app/src/utils/access.ts b/apps/readest-app/src/utils/access.ts
index 5b69e2ad..fd1a24d1 100644
--- a/apps/readest-app/src/utils/access.ts
+++ b/apps/readest-app/src/utils/access.ts
@@ -65,7 +65,7 @@ export const isCloudSyncInPlan = (plan: UserPlan): boolean =>
  * Every gate goes through {@link isCloudSyncAllowed}, so this flag is the
  * whole toggle.
  */
-export const CLOUD_SYNC_REQUIRES_PREMIUM = true;
+export const CLOUD_SYNC_REQUIRES_PREMIUM = false;
 
 /**
  * Whether third-party cloud sync is available for a plan. Falls back to the
